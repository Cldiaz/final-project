<nav class="navbar navbar-transparent">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">MANAGE SUPPORT</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">    
      <ul class="nav navbar-nav navbar-right">
        <!-- Not logged in -->
        <% if(!displayName && title === "Login") { %> 
            <li><a href="/register">Register</a></li> 
            
        <!-- If they are login as Admin -->
        <% } else if(displayName && type === "Admin") { %>
        
        <!-- ADD ACTIVE NAVBAR PART LATER -->
        
                <li class="active"><a href="/users">Users</a></li>
                <li><a href="/tickets">Dashboard</a></li>
                
        <!-- If they are login as Customer-->        
        <% } else if(displayName && type === "Customer") { %>
        
        
        <!-- ADD ACTIVE NAVBAR PART LATER -->
        
        
                <li class="active"><a href="/mytickets">My Tickets</a></li>
                <li><a href="/users/edit">My Account</a></li>
            
         <% } if(displayName){ %>
                <li><a href="/contact">Contact Us</a></li>
                <li class="navbar-text"><i class="fa fa-user"></i> Wecome, <%= displayName %></li>
                <li><a href="/logout"><i class="fa fa-sign-out"></i> Logout</a></li>
         <% } else { %>
                <li><a href="/contact">Contact Us</a></li>
                <li><a href="/login"><i class="fa fa-sign-in"></i> Login</a></li>
         <% } %>
               
            
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>